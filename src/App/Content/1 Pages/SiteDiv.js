import styled from "styled-components";

const SiteDiv = styled("div")`
    position: absolute;
    width: 100%;
    height: 100%;
    background: linear-gradient(black 50%, grey);
`;

export default SiteDiv;